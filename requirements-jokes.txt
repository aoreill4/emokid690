# Deps for segmenting transcripts into jokes via the Claude API
# (src/segment_jokes.py).
#
#   py -m pip install -r requirements-jokes.txt
#
anthropic>=0.40.0
pandas>=2.0.0
pyarrow>=14.0.0
python-dotenv>=1.0.0
